#include <stdio.h>

int main(void) {
	int a,i,d=0;
	printf("enter n.o");
	scanf("%d",&a);
	for(i=1;i<=a;i++)
{
	if(a%i==0)
	d++;
}
if(d==0)
{
	printf("yes");
}
else
{
	printf("no");
}

	
	return 0;
}
